
/* some. Es como .find, pero pregunta   */
let array =[1,, 3, 8, 9, 11];

console.log(numeros.some(function(n){
    return (n%2==0)  // El número es par
}
));

// coin -> concatena arrays
let todo = numeros.concat(letras);
console.log(todo);

//includes-> Indica si existe un elemento en un array
console.log(numeros.include(8));
console.log(numeros.includes(7));

// join -> 
//join();

// reduce -> mezcla cosas de un array


// @slice -> rodaja o porción



/* 
Ejercicio. Obtener una lista con los nombres de las mascotas que tienen la extensión jpg
La nueva lista no debe tener la extensión, sólo los nombvres. 
Nombres en mayúsculas. 
Utiliar slice, map y filter. 
*/
let ficheros = ['perrito.jpg', 'gatito.avi','loro.mp3','galápago.docs','gallo.jpg','',''];

